import { Text, View } from 'react-native'
import React, { Component } from 'react'

export class OpenAviatorGame extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  componentWillMount() {
    // window.getAviator && window.getAviator()
  }

  render() {

    return (
      <View>
        {/* <Text>OpenAviatorGame</Text> */}
      </View>
    )
  }
}

export default OpenAviatorGame